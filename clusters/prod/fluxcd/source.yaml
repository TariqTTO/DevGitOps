apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: datauniverse-gitops
  namespace: flux-system
spec:
  interval: 1m
  url: ssh://git@github.com/TariqTTO/DevGitOps.git
  secretRef:
    name: flux-system
  ref:
    #!# branch: main
    branch: init-tto/gitops
