apiVersion: v1
kind: ResourceQuota
metadata:
  name: datauniverse-quota
  namespace: datauniverse-prod
spec:
  hard:
    requests.cpu: "8"
    requests.memory: 16Gi
    limits.cpu: "16"
    limits.memory: 32Gi
    persistentvolumeclaims: "20"
    pods: "50"
    services: "15"